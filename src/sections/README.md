# Sections

Landing page sections should live here once the new Figma direction is provided.

Each section should be self-contained, responsive, and composed from reusable components where practical.
