# Assets

Project-local source assets can live here when they are imported by React components.

The preserved exported assets from the previous landing page remain in `public/figma-assets` and are indexed in `src/data/projectDetails.js`.
